# AI 시계열 하이브리드 모델 방법론 연구 보고서

**프로젝트명**: Chronos+MTGNN 하이브리드 시계열 모델 기반 KOSPI 포트폴리오 운용
**기간**: 2026년 4월 ~ 현재
**담당**: 권승빈 (AI 시계열 파트)

---

## 1. 연구 개요

KOSPI 전 종목(781개)의 OHLCV + 기술적 지표를 입력으로, 딥러닝 시계열 백본이 추출한 임베딩을 PPO 강화학습 에이전트에게 전달해 클러스터 단위 포트폴리오 비중을 결정하는 파이프라인을 구축했다.

```
[데이터 수집 → 전처리 → 주파수 피처 → 클러스터링]
          ↓
[시계열 백본 비교 → 하이브리드 모델 → 임베딩 추출]
          ↓
[PPO 강화학습 → 포트폴리오 운용 → 백테스트]
```

---

## 2. 단계별 방법론 및 결과

### 2-1. 데이터 수집 (4월)

KRX 상장 KOSPI 종목 코드 전수를 대상으로 OHLCV와 20개 기술적 지표를 수집했다. 초기 837개 종목 중 데이터 길이 500일 미만, 거래량 결측 10% 초과 종목을 제거해 **781개 유효 종목**을 확보했다.

| 항목 | 내용 |
|------|------|
| 수집 기간 | 2020-01-01 ~ 2026-05-04 |
| 총 행 수 | 약 118만 행 |
| 기술적 지표 | SMA, EMA, MACD, RSI, 볼린저밴드, ATR, 변동성(20일), Volume 비율 등 |
| 보조 변수 | BTC/ETH 가격, 미국 10년 국채 금리, KOSPI 지수 |

**시행착오**: 종목 코드 포맷 문제(앞자리 0 누락)와 yfinance API 호출 실패가 반복되어, 배치 처리 + 재시도 로직을 별도로 구현했다. 거래 정지·투자유의 종목은 거래량=0 구간이 길어 자동 필터링이 필요했다.

---

### 2-2. 전처리: RevIN + 패치 시퀀스 생성 (5월)

주가는 종목마다 절대 가격 수준이 다르고 비정상(non-stationary) 시계열이다. 이를 해결하기 위해 **RevIN(Reversible Instance Normalization)** 을 도입했다: 60일 입력 구간별로 독립적으로 정규화하고, 예측 후 역변환으로 원래 스케일을 복원한다.

슬라이딩 윈도우로 "과거 60일 → 미래 5일 수익률" 입력-출력 쌍을 생성했다.

```
SEQ_LEN=60, PRED_LEN=5, PATCH_LEN=5 (5일=1패치, 12패치)
총 시퀀스: 학습 ~80만, 검증 ~10만, 테스트 ~10만 개
```

상승/하락 비율이 50:50으로, "항상 상승 예측" 편법으로 50%를 얻을 수 없는 데이터 구조임을 확인했다.

**시행착오**: 100만 개 시퀀스를 메모리에 한 번에 올리는 것이 불가능해 Colab이 반복적으로 OOM 크래시됐다. HDF5 포맷으로 전환해 배치 단위 스트리밍 로드 방식을 채택했다.

---

### 2-3. 주파수 도메인 피처 추출 (6월)

주가 시계열에는 단기 노이즈, 중기 추세, 장기 사이클이 혼재한다. 이를 분리하기 위해 두 가지 변환을 적용했다.

**FFT (고속 푸리에 변환, 13개 피처)**: 전체 구간에서 어떤 주파수 성분이 강한지 추출. 단, 시간 정보가 사라지는 한계가 있다.

**DWT (이산 웨이블릿 변환, 16개 피처, db4 Level-3)**: 시간-주파수를 동시에 분석. cA3(장기 추세), cD3/cD2/cD1(중·단기 변동)의 통계량을 추출한다.

LightGBM으로 유효성을 빠르게 검증했다:

| 실험 | ROC-AUC | 비고 |
|------|---------|------|
| Baseline (기술지표 요약 40개) | 0.5121 | — |
| + DWT (16개) | 0.5227 | **+0.0106** |
| + FFT + DWT (29개) | 0.5250 | **+0.0129** |
| + FFT만 추가 | 0.5087 | **-0.0034 (역효과)** |

**핵심 해석**: FFT 단독은 시간 정보가 없어 금융 시계열에서 오히려 노이즈로 작용했다. DWT가 금융 비정상 시계열에 더 적합하다. LightGBM의 best_iter가 14~32로 매우 낮게 나타나, 요약 통계값보다 **시퀀스 전체를 직접 처리하는 딥러닝 모델의 필요성**을 확인했다.

---

### 2-4. DTW K-Means 클러스터링 (7월)

업종 분류는 사업 내용 기준이지 주가 패턴 기준이 아니다. 실제 가격 움직임이 유사한 종목 그룹을 발견하기 위해 DTW 거리를 사용한 K-Means 클러스터링을 수행했다.

**DTW(Dynamic Time Warping)**: 두 시계열의 시간 이동(time shift)을 허용해 패턴 유사도를 측정하는 거리 척도.

**시행착오**: DTW는 O(T²) 복잡도로, 700포인트 일별 시계열 그대로 사용하면 수 시간이 소요됐다. 월별 35포인트로 다운샘플링해 약 400배 속도를 단축했다. K=5~20 범위 탐색 중 최솟값 K=5에서 최고 실루엣 점수가 나왔다.

**결과 (K=5, 757개 종목)**:

| 클러스터 | 종목 수 | 주요 업종 | 특징 |
|---------|--------|----------|------|
| C0 | 108 | 부동산, 기타 금융, 플라스틱 | 느린 추세 추종 |
| C1 | 112 | 철강, 자동차 부품, 금융 | 일중 변동성 민감 |
| C2 | 118 | 자동차 부품, 금융, 제약 | 수급 변화 민감 |
| C3 | **299** | 의약품, 금융, 자동차 부품 | 시장 평균 동행 (40%) |
| C4 | 120 | 금융, 화학, 보험 | 추세+변동성 복합 |

C3이 전체의 40%를 차지하며 시장 지수와 동행하는 "중간형" 종목군임을 확인. 이 클러스터 정보는 이후 MTGNN의 그래프 인접 행렬 및 RL 상태(state) 정의에 활용됐다.

---

### 2-5. 시계열 백본 비교 1차 (8월) — 4개 모델

동일한 데이터(`sequences.h5`, 학습 10만 개)로 4개 모델을 공정하게 비교했다.

| 모델 | Dir Acc | Spearman | MAE | 특징 |
|------|---------|----------|-----|------|
| Chronos (zero-shot) | **0.5096** | **0.0192** | ※무효 | Amazon T5 사전학습, 추가 학습 없음 |
| iTransformer | 0.5057 | 0.0056 | **0.04330** | 피처 축 토큰화, Inverted Attention |
| StockMixer | 0.5033 | 0.0072 | 0.04515 | MLP 기반 경량 모델 |
| PatchTST | 0.4960 | 0.0096 | 0.04392 | 패치 기반 Transformer |
| 랜덤 베이스라인 | 0.5000 | 0 | — | — |

> ※ Chronos MAE/RMSE는 RevIN 정규화된 가격 공간에서 예측하므로 수익률 단위와 스케일이 달라 비교 불가

**핵심 발견**: 추가 학습 없는 Chronos가 방향성 1위 → 대규모 사전학습의 위력. iTransformer는 피처 간 상관관계(MACD↔RSI 등)를 포착해 MAE 최저.

**시행착오**: PatchTST가 랜덤 이하(49.60%)를 기록했다. 10 에폭, 10만 개 샘플의 한계일 수 있지만, 이 실험의 목적은 정밀 튜닝이 아닌 백본 선정이므로 결론 도출에 충분하다고 판단했다.

---

### 2-6. iTransformer 어텐션 해석 (8월)

iTransformer의 variate 어텐션 맵(20×20 피처×피처)을 DTW 클러스터별로 분리해 분석했다.

**전체 클러스터 공통 상위 피처 (어텐션 수신량 기준)**:

| 순위 | 피처 | 의미 |
|------|------|------|
| 1 | MACD_hist | 단기 모멘텀 변화율, 추세 전환 신호 |
| 2 | RSI_14 | 과매수/과매도 수준 |
| 3 | Return_5d | 단기 수익률 모멘텀 |
| 4 | Volume_ratio | 수급 변화 신호 |
| 5 | Return_1d | 당일 가격 충격 |

**발견**: 절대 가격(OHLC)보다 **파생 지표(모멘텀, 변동성, 거래량)**에 더 높은 어텐션이 집중됐다. 클러스터별 미세한 패턴 차이도 확인됐다 (C1: 가격 범위 민감, C2: 거래량 민감 등).

---

### 2-7. 확장 백본 비교 2차 (8.5월) — 7개 모델

기존 4개 모델에 MTGNN, Mamba, Chronos-LoRA 3개를 추가해 비교했다.

| 모델 | Dir Acc | Spearman | MAE | 비고 |
|------|---------|----------|-----|------|
| **MTGNN** | **0.5145** | **0.0356** | **0.043054** | 그래프 + Gated TCN |
| Chronos (zero-shot) | 0.5096 | 0.0192 | ※무효 | |
| iTransformer | 0.5057 | 0.0056 | 0.043301 | |
| Chronos-LoRA | 0.5038 | **-0.0079** | 0.043176 | LoRA 파인튜닝 역효과 |
| StockMixer | 0.5033 | 0.0072 | 0.045154 | |
| BiGRU (Mamba 대체) | 0.4987 | 0.0001 | 0.043912 | |
| PatchTST | 0.4960 | 0.0096 | 0.043922 | |

**MTGNN이 전 지표에서 1위**: 방향성 51.45%, Spearman 0.0356으로 이전 1위 모델들을 모두 앞섰다. KOSPI 종목 간 그래프 관계(DTW 클러스터 기반 인접 행렬)를 Adaptive Graph Learner로 자동 학습하는 구조가 효과적이었다.

**주요 시행착오 2건**:

**① Chronos-LoRA 역효과**: Chronos에 LoRA(r=8, Q/V 어텐션, lr=2e-4)를 적용해 파인튜닝했더니 Spearman이 zero-shot 대비 **+0.0192 → -0.0079로 오히려 하락**했다. 사전학습으로 내재화된 시계열 패턴 지식이 상대적으로 작은 금융 데이터 파인튜닝으로 방해받는 **Catastrophic Forgetting** 현상으로 해석된다.

**② Mamba → BiGRU 대체**: mamba-ssm 패키지가 Colab T4 CUDA 환경과 호환되지 않아 설치 자체가 실패했다. SSM과 구조적으로 유사한 BiGRU로 대체했으나 성능(49.87%)이 기대에 미치지 못했다. 실제 Mamba가 더 나올 가능성이 있으나 환경 제약으로 검증 불가였다.

---

### 2-8. 멀티모달 방향 취소 → RL 파이프라인으로 전환 (5월 방향 변경)

초기 계획은 **시계열 + 뉴스 임베딩** 멀티모달이었다. 그러나 한국 주식 뉴스 데이터를 충분히 확보하지 못했다.

- 유료 API(KRX, 연합뉴스 등) 접근 불가
- 무료 크롤링으로는 781개 종목 × 수년치 뉴스 매핑이 현실적으로 불가능
- 텍스트-주가 시간 정렬 문제 (장 마감 후 뉴스 vs 다음날 반영 등)

이에 따라 **PPO 강화학습으로 클러스터 단위 포트폴리오 비중을 결정**하는 방향으로 전환했다.

---

### 2-9. 하이브리드 모델 구현 (9월)

백본 비교 결과를 반영해 최종 하이브리드 아키텍처를 **Chronos T5 인코더(temporal 패턴) + MTGNN(그래프 기반 cross-stock 관계)** 으로 확정했다.

```
입력: (B, 12패치, 5일/패치, 20피처)
        ↓
[Chronos T5 인코더]  — 시간 축 패턴 (추세, 주기성)
[MTGNN]             — 종목 간 그래프 관계 (Adaptive Graph Learner)
        ↓ Fusion Layer
64-dim 임베딩 (종목 × 날짜)
        ↓
rl_embeddings.h5 → PPO 강화학습
```

**RL 환경 설계**:

| 항목 | 내용 |
|------|------|
| State | 5클러스터 평균 임베딩(5×64) + 현재 비중(5) = 325-dim |
| Action | Softmax 정규화된 클러스터 비중 (5-dim) |
| Reward | 일일 포트폴리오 수익률 − 거래비용(0.1%) |
| 학습 / 테스트 | 2021~2023 / 2024~2025 |

---

### 2-10. PPO 강화학습 백테스트 결과 — 치명적 시행착오

| 전략 | 총 수익률 | 연환산 수익률 | Sharpe | MDD |
|------|---------|-------------|--------|-----|
| **PPO (Chronos+iTransformer)** | 0.28% | 0.14% | 0.100 | -27.21% |
| **등가중 벤치마크** | **34.33%** | **16.57%** | **1.043** | -13.60% |

**PPO가 등가중 벤치마크에 완패했다.** 2년 테스트 기간 동안 단 0.28% 수익률에 MDD -27.21%는 단순 균등 분산(34.33%, Sharpe 1.043) 대비 사실상 무의미한 수준이다.

**실패 원인 분석**:

1. **State 차원 과부하**: 325-dim 상태 공간에 PPO가 수렴하기에 학습 데이터(2021~2023, 약 500 에피소드)가 부족했을 가능성이 높다.
2. **Reward 신호 희소성**: 일일 수익률 기반 보상은 노이즈가 매우 크다. 포트폴리오 최적화에서는 Sharpe 기반 리워드나 risk-adjusted 보상이 필요하다.
3. **도메인 시프트**: 학습 기간(2021~2023) 시장 특성(코로나 반등, 금리 인상기)이 테스트 기간(2024~2025)과 크게 달라 정책 일반화에 실패했다.
4. **임베딩 품질 미검증**: 하이브리드 모델 임베딩이 실제로 종목의 미래 수익률과 상관관계가 있는지 별도 검증 없이 RL에 바로 투입됐다.
5. **클러스터 단위 action의 한계**: 5개 클러스터 비중만 조정하는 것은 종목 내 분산 효과를 전혀 활용하지 못한다.

---

## 3. 방법론 진화 흐름 요약

```
단순 통계 피처 (LightGBM)
  → AUC 0.525, 주파수 피처 유효성 확인, 딥러닝 필요성 근거 확보

단일 백본 비교 (StockMixer / PatchTST / iTransformer / Chronos)
  → Chronos zero-shot이 학습 모델 압도 → 사전학습 중요성 확인

확장 백본 비교 (MTGNN / Mamba / Chronos-LoRA 추가)
  → MTGNN 전 지표 1위, Chronos-LoRA 역효과, Mamba 환경 실패

하이브리드 모델 (Chronos+MTGNN) + PPO
  → PPO 완패 (벤치마크 대비 Sharpe 0.100 vs 1.043)

[현재 위치: RL 파이프라인 재설계 필요]
```

---

## 4. 전체 실험 결과 요약

| 단계 | 방법 | 핵심 결과 |
|------|------|----------|
| 6월 주파수 특징 | FFT + DWT → LightGBM 검증 | AUC 0.525, DWT 유효 / FFT 단독 역효과 |
| 7월 클러스터링 | Soft-DTW K-Means (K=5) | KOSPI를 5개 패턴 그룹으로 분류 성공 |
| 8월 백본 1차 | 4개 모델 비교 | Chronos zero-shot 50.96% 1위, iTransformer MAE 최저 |
| 8월 어텐션 분석 | iTransformer × DTW 5클러스터 | MACD_hist, RSI_14, Return_5d 상위 피처 |
| 8.5월 백본 2차 | 7개 모델 비교 | MTGNN 51.45% / Spearman 0.0356 전 지표 1위 |
| 9월 하이브리드 | Chronos+MTGNN Fusion | 64-dim 임베딩 추출, rl_embeddings.h5 생성 |
| 10월 RL | PPO 포트폴리오 운용 | 총 수익률 0.28% vs 벤치마크 34.33% — **완패** |

---

## 5. 핵심 교훈

**잘 된 것**:
- MTGNN의 그래프 기반 종목 간 관계 모델링이 순수 시계열 모델보다 효과적
- DWT 주파수 피처가 FFT보다 금융 데이터에 적합함을 실험으로 검증
- DTW 클러스터링으로 업종과 무관한 시장 내 실제 움직임 그룹 발견
- iTransformer 어텐션 분석으로 모델 해석 가능성(XAI) 확보

**시행착오로부터 얻은 교훈**:
- Chronos 파인튜닝은 신중해야 한다 — zero-shot 성능이 충분하면 파인튜닝이 역효과
- OOM 문제는 아키텍처 설계 단계에서 HDF5 스트리밍을 전제로 고려해야 한다
- 강화학습의 보상 함수를 단순 수익률로 설계하면 금융 노이즈에 취약하다
- 임베딩을 RL에 투입하기 전 임베딩 품질 자체를 별도 검증해야 한다

**향후 과제**:
1. Reward를 Sharpe 비율 기반으로 재설계
2. State 차원 축소 (PCA 또는 임베딩 차원 32로 감소)
3. 종목별 action 공간으로 확장 (클러스터 단위 → 종목 단위)
4. 임베딩-수익률 상관관계 사전 검증 후 RL 재학습

---

*GitHub: https://github.com/winbean-kwon/ToBeRich*
