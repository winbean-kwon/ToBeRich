# 강화학습(RL) 트랙 진행 기록 — 증권사 연동 시도부터 크립토 HRL까지 (2026-07-10 ~ 07-24)

> GAPS ETF 대회 트랙(딥러닝+인간 뷰 BL/MVO, RL 미사용)과는 별개로 진행된 졸업 프로젝트 본 트랙의 기록이다.

기존 KOSPI 일봉 PPO가 초기 실험에서 등가중 대비 크게 실패(PPO +0.28% vs 등가중 +34.33%, `progress_report_final.md` §2.4)한 뒤, 이를 넘어서기 위해 "분봉 단위 실거래까지 가능한 RL 시스템"으로 방향을 바꾸었고, 그 뒤 2주 동안 증권사 연동 시도 좌절, 자산군 전환, 단일 PPO 재실패, EarnHFT 계층형 강화학습(HRL)의 네 차례 반복, 그리고 이와 병행한 BTC 단일자산 트랙까지 이어지는 시행착오가 있었다. 아래에서는 이 흐름을 시간 순서대로 서술한다.

---

## Phase 1. 증권사 연동 1차 시도 — KOSPI 분봉 실거래 (2026-07-10 ~ 07-13, 중단)

가장 먼저 시도한 것은 기존 KOSPI 일봉 PPO의 부진한 결과를 개선하기 위해 알파 신호 자체를 5분봉 피처로 전면 교체하고, 나아가 실계좌 실거래까지 가능한 환경을 구축하는 것이었다. 그런데 한국투자증권(KIS) Open API의 분봉조회(`FHKST03010200`)는 당일 데이터만 제공하고 과거 분봉 히스토리를 제공하는 API가 없다는 제약이 있었다. 이를 우회하기 위해 히스토리 백필은 대신증권 크레온 Plus(Windows COM 전용, 약 5년치 5분봉)로, 실거래 실행은 KIS REST API(Mac/Linux에서도 가능)로 서로 다른 브로커에 역할을 나누는 구조를 설계했다. 이 설계에 따라 `collect_minute_data_creon.py`(Windows 전용, 미실행), `broker/base.py`와 `broker/kis_client.py`(TR_ID는 미검증 초안), `live_trading_loop.py`(기본 dry-run 상태로 EqualWeightPolicy를 통한 브로커 연동만 검증 가능), `.env.example`(KIS 인증정보 템플릿) 등의 파일을 만들었다.

그러나 2026년 7월 13일, 크레온이 Windows COM에 강하게 의존한다는 사실이 실제로 확인되면서 Mac 환경에서는 히스토리 백필 자체가 원천적으로 불가능하다는 결론에 도달했다. 이 선행 조건이 막히자 분봉 재학습과 KIS 실거래 브릿지를 포함한 계획 전체가 사실상 보류됐다. 결국 KOSPI 트랙의 최종 산출물은 기존 일봉 PPO(`rl_trading_colab.ipynb`)로 확정하기로 했고, 다만 "분봉 단위 실거래"라는 아이디어 자체는 폐기하지 않고 자산군을 바꾸어 다음 단계로 이어가기로 했다.

---

## Phase 2. 크립토(Binance)로 자산군 전환 (2026-07-13 ~ 07-14)

크레온의 Windows 의존성 문제를 근본적으로 피하기 위해, 히스토리 분봉 무료 API와 실거래 REST API를 모두 Mac에서 사용할 수 있는 Binance로 자산군 자체를 전환하기로 했다. ccxt 기반의 `crypto/broker/base.py`와 `crypto/broker/binance_client.py`(quantity는 float, 기본 테스트넷), 거래대금 상위 N개 USDT 현물 종목을 동적으로 산출하는 `crypto/universe.py`, 5분봉 히스토리를 백필하는 `crypto/collect_minute_data_binance.py`(Windows 불필요, API 키 없이도 동작), 24/7 게이트 없이 EqualWeightPolicy만 구현된 `crypto/live_trading_loop_binance.py`, 그리고 `crypto/.env.example`을 새로 만들었다. ccxt 설치와 Binance 공개 API 호출(유니버스 조회, BTC/USDT 1시간치 5분봉 fetch)은 실전 검증까지 마쳤다.

이때 중요한 스코프 결정을 내렸는데, 크립토 트랙은 KOSPI 트랙을 대체하는 것이 아니라 병행 실험이라는 점이다. KOSPI는 일봉 PPO로 종료된 채로 유지하고, 분봉·실거래 실험은 크립토 트랙에서만 이어가기로 했다 — 즉 두 트랙은 별개 산출물이며 크립토 쪽 작업이 KOSPI 논문 파트를 대체하지 않는다.

---

## Phase 3. 데이터·피처·하이브리드 백본 파이프라인 (2026-07-14)

50개 심볼의 5분봉 데이터를 2021년 1월 1일부터 백필해 `crypto/data/minute_ohlcv_raw/`에 저장했고, `preprocess_features.py`로 전처리해 KOSPI와 동일한 기술지표 20개를 5분 그리드에 정합화한 `crypto/data/features_5m/`(44심볼, 약 1,770만 봉)을 만들었다. 이 과정에서 타겟은 실거래 루프의 리밸런싱 주기(30분)와 맞춘 6봉 뒤 수익률 `RetTarget_6b`로 확정했고, 유니버스는 히스토리 6개월 미만인 6종목을 제외한 44종목으로 정했으며, FFT/DWT나 클러스터링 같은 고급 피처는 나중으로 미루고 우선 기술지표 20개만으로 end-to-end 파이프라인을 완성하기로 했다. 마지막으로 KOSPI에서 쓰던 Chronos+MTGNN 하이브리드 백본을 `crypto_hybrid_model_colab.ipynb`로 이식해(sequences.h5 없이 노트북 안에서 윈도우 샘플링하는 방식으로) `crypto_hybrid_best.pt`와 종목×30분 스텝 단위 64차원 임베딩인 `crypto_rl_embeddings.h5`를 생성했다.

---

## Phase 4. 단일 PPO — 실패

`crypto_rl_trading_colab.ipynb`에서 KOSPI와 동일한 구조로 단일 PPO 정책을 학습시켰지만, 테스트 구간(2026년 1~7월)에서 총수익률 -75.68%(연환산 -92.99%, Sharpe -4.209, MDD -79.04%)라는 큰 실패를 기록했다. 같은 기간 등가중 벤치마크는 +2.66%였으니, 단일 PPO는 벤치마크에도 한참 못 미친 셈이다.

---

## Phase 5. EarnHFT 계층형 강화학습(HRL) 이식 — 44종목 포트폴리오 트랙

단일 PPO의 실패를 계기로 EarnHFT(Qin et al., 2024) 논문의 계층 구조 — 레짐 분할, 레짐별 에이전트 풀, 일 단위 라우터 — 를 이식하기로 했다. `crypto/notebooks/crypto_hrl_earnhft_colab.ipynb` 한 노트북 안에서 v1부터 v4까지 계속 진화시켰다.

**v1(최초 이식)**은 시장 레짐을 나누는 Stage A, 선호도(β = -90, -30, 30, 90) 4개 에이전트 풀을 학습하는 Stage B, 일 단위로 풀을 선택하는 DQN 라우터를 학습하는 Stage C로 구성했다. 테스트 결과는 비용 후 -32.24%였는데, 턴오버와 거래비용을 진단해보니 스텝당 평균 턴오버가 5.4%, 누적 비용 드래그가 -39.81%로 손실의 거의 전부가 거래비용에서 나온다는 것을 확인했다. 실제로 비용을 역산하면 비용 전 알파는 +12.6%로 이미 등가중을 이기고 있었다 — 즉 신호 자체는 있는데 30분 단위로 너무 자주 리밸런싱하다 보니 비용이 그 알파를 전부 태우는 구조였다.

이 문제를 풀기 위해 재학습 없이 추론 단계에서만 `w_new = (1-α)·w_old + α·softmax(action)` 형태의 관성 블렌딩을 적용하는 **스무딩(§9)**을 시도했다. α를 0.3부터 0.01까지 스윕한 결과 α=0.01에서 비용 후 총수익 +11.05%, Sharpe 0.638, MDD -36.47%를 기록해 등가중(+2.66%, 0.367, -40.09%)을 확실히 앞질렀고, 이 구성이 지금까지도 최종 확정 구성으로 남아 있다. 비용 전 알파가 α 값과 무관하게 12.5~12.6% 수준으로 거의 변하지 않았다는 점은 이 신호가 수일 단위의 저빈도 신호라는 근거로 해석했다.

다음으로는 이 스무딩 효과를 아예 학습 과정 자체에 내장하면 어떨까 싶어 **v2(§10)**를 시도했다. `SmoothStepMixin`으로 관성(α=0.03)을 학습 환경에 넣고 라우터에는 시장 피처까지 추가해 재학습했지만 결과는 실패였다 — 비용 후 -3.58%, 비용 전 알파마저 12.6%에서 0.92%로 소멸해버렸다. 여기서 얻은 핵심 교훈은 "학습은 전권(α=1)으로, 실행 단계에서만 관성을 걸어야 한다"는 것이다. 관성을 학습 환경 안에 넣으면 행동과 보상 사이의 연결이 희석되어 PPO의 credit assignment 자체가 무너지기 때문이다. 이는 보고서에 실을 만한 부정적 ablation 결과로 남겨두었다.

이후 EarnHFT 논문에 더 충실하게 재구현해보자는 취지로 **v3(§12)**를 진행했다. 현금을 45번째 액션으로 추가하고, Stage II 선별과 Stage III 검증을 논문대로 2025년 데이터를 train/valid로 엄격히 분할해 수행함으로써 테스트 구간에서의 선택 편향까지 제거했다. 그럼에도 결과는 비용 후 +0.68%로 등가중에도 미치지 못했다. 원인을 들여다보니 현금 비중이 테스트 전 구간에서 2.2%(=1/45 균등)로 고정되어 있었다 — 즉 에이전트들이 사실상 균등가중 정책으로 수렴해버렸고 현금이라는 액션 자체를 전혀 학습하지 못한 것이다. 실제로 20개 후보의 레짐별 성적을 비교해보니 서로 1%p도 차이가 나지 않는 클론 수준이었다. 여기서 얻은 교훈은 EarnHFT의 다양성이 사실 Stage I(DP 기반 Q-teacher)이 β를 직접 지도 신호로 주입하기 때문에 생기는 것이지, β를 단순히 데이터 샘플링 우선순위로만 쓰면(목적함수 자체는 그대로이므로) 45차원 연속 액션을 다루는 PPO에서는 다양성이 자연히 생기지 않는다는 점이었다.

v3의 이 실패를 Stage I 없이도 우회할 방법을 고민하다가 **v4(§13)**로 이어졌다. 44차원 연속 softmax 대신 top-K(=10) 종목만 골라 그 안에서 배분하는 FreQuant 방식의 희소 선택과, 종목 선택과는 분리된 전용 리스크게이트 ρ를 두는 DeepClair 방식을 저수준 행동공간 자체에 구조적으로 이식한 것이다. 이 구조 변경은 다양성 문제를 확실히 해결했다 — ρ의 표준편차가 0.068로(v3는 사실상 0이었다) 실제로 시간에 따라 변했고, β별 평균 ρ에도 0.109에서 0.545까지 뚜렷한 순서가 생겼으며, top-10 종목도 테스트 기간 동안 44개 중 33개까지 돌아가며 선택됐다. 다만 성능은 절충적이었다. 스무딩을 걸지 않은 raw 정책의 비용 전 알파가 -0.60%로 사실상 0에 가까웠다는 점에서, v4의 원시 저수준 정책 자체는 v1보다 오히려 약하다는 것을 알 수 있었다. 여기에 v1과 동일한 관성 스무딩을 적용하면 α=0.01에서 비용 후 +8.26%, Sharpe 2.168, MDD -5.22%를 기록해, 총수익은 v1+스무딩의 +11.05%보다 낮지만 Sharpe와 MDD는 훨씬 우수한 결과를 얻었다. 이 결과가 혹시 "초반에 우연히 잡은 포지션이 그대로 버틴" 정적 아티팩트가 아닐지 의심되어 별도의 진단을 추가로 만들어 확인했는데, 턴오버가 테스트 마지막까지 죽지 않고 계속 유지되었고(마지막 10% 구간에서 턴오버가 0인 스텝은 0.0%였다), 전반부(+5.47%)와 후반부(+2.64%) 모두 플러스 수익을 기록했으며, 종목 선택의 자카드 유사도도 0.26으로(앞 1/4 구간과 뒤 1/4 구간의 종목이 절반 이상 교체됨) 낮게 나와, 이 결과가 아티팩트가 아니라 실제로 살아 있는 신호라는 결론을 내릴 수 있었다.

---

## Phase 6. BTC 단일자산 EarnHFT 완전 이식 (병행 트랙, 2026-07-18~)

v3에서 얻은 "44종목 연속 비중 세팅에서는 Stage I(DP Q-teacher)이 원리상 불가능하다"는 결론에 따라, 이번에는 논문 설정 그대로 단일자산(BTC)·이산 포지션으로 축소해 Stage I(DP)부터 Stage III(라우터)까지 3단계를 전부 구현하는 별도 노트북(`crypto_earnhft_btc_colab.ipynb`)을 새로 만들었다. 이 트랙은 44종목 포트폴리오 트랙과는 완전히 별개의 산출물로 병행해서 진행했다.

**v1**은 시스템 전체를 처음으로 이식한 버전인데, 결과는 -135.45%로 나왔다. 다만 이는 실제 신호의 문제라기보다는 가산(additive) 방식의 NAV 회계가 -100% 아래로 뚫려버려 수치 자체가 무의미해진 것이었다. 그래도 다양성 진단만큼은 통과했다 — β별 평균 포지션이 0.39에서 0.79까지 순서 있게 나타나, v3에서 안 됐던 다양성 확보 자체는 이 트랙에서 성공한 셈이다. 이 회계 문제를 고치기 위해 **v2**에서는 복리 NAV 방식과 함께, Q값 차이가 일정 임계값(κ)을 넘을 때만 포지션을 바꾸는 κ 히스테리시스를 도입했다. κ=1.0에서 결과는 -14.66%로, 바이앤홀드(-28.5%)는 넘어섰지만 절대 수익은 여전히 음수였다. 다만 κ=0(무제약)일 때의 결과와 비교하면 83%p나 개선된 것이어서, 회전 억제가 결정적인 기여를 한다는 점은 명확히 확인했다. 이때 남은 병목은 라우터(Stage III)였는데, valid 구간에서 전 체크포인트가 아무 행동도 하지 않는 것보다 나쁜 성적을 보였다.

라우터 문제를 풀어보고자 학습 없이 트레일링 기울기로 레짐을 판정하는 규칙 기반 라우터를 **v2.1**로 시도했지만, 결과는 -31.04%로 오히려 DDQN 라우터(-22.48%)보다도 나빴다. 원인은 판정 경계가 세그먼트 기울기의 분위수로 캘리브레이션되어 있었는데 실제 트레일링 통계와 스케일이 맞지 않아, 전체 구간의 44%를 잘못된 레짐 에이전트에 위임해버린 것이었다. 이 경계를 트레일링 통계 자체의 분위수로 다시 보정한 **v2.2**는 아직 결과를 기다리고 있는 상태다. 참고로 이 과정에서 동일한 코드와 시드로 DDQN 라우터를 재실행했더니 -14.66%에서 -22.48%로 결과가 옮겨가는 것을 확인했는데, 이는 GPU 연산의 비결정성 때문으로 보이며, 라우터 학습 결과는 ±8%p 수준의 노이즈를 갖는다는 뜻이라 보고서에는 단일 실행 관측치로 명시해야 한다.

---

## 공통 핵심 교훈

두 트랙을 거치며 반복적으로 확인된 교훈이 세 가지 있다. 첫째, 거래비용은 거의 항상 1차 용의자였다. 5분봉·30분 리밸런싱 환경에서는 비용 전/후 성과를 항상 먼저 나누어 봐야 했고, 대부분의 "실패"는 알파가 없어서가 아니라 회전을 억제하지 못해서였다(44종목 트랙의 v1과 BTC 트랙의 v1이 똑같은 패턴을 보였다). 둘째, "학습은 전권으로, 실행에서만 관성을 건다"는 원칙이 두 트랙에서 독립적으로 재확인됐다 — 회전 억제를 추론 단계(사후 스무딩이나 κ 히스테리시스)에서 적용하면 성공했고, 학습 환경 자체에 넣으면(v2) credit assignment가 무너져 실패했다. 셋째, EarnHFT의 "다양성"은 Stage I의 DP 기반 Q-teacher에서 나온다는 점이다. Q-teacher 없이 β 샘플링만으로는(v3) 연속 액션 공간에서 다양성이 생기지 않으며, 이를 우회하려면 Q-teacher를 실제로 구현하거나(BTC 트랙) 저수준 행동공간 자체를 구조적으로 제약해야 한다(v4의 top-K+게이트 방식).

## 현재 확정 산출물과 다음 단계

지금 시점에서 44종목 포트폴리오 트랙의 최종 확정 산출물은 v1 풀+라우터에 추론 시 α=0.01 관성 스무딩을 적용한 구성으로, 총수익 +11.05%, Sharpe 0.638, MDD -36.47%를 기록해 등가중(+2.66%, 0.367, -40.09%)을 넘어선다. v4(FreQuant+DeepClair)는 총수익은 이보다 낮지만 Sharpe 2.168, MDD -5.22%로 리스크 지표가 훨씬 우수한 대안이라, 보고서에는 "구조적 다양성 문제를 해결한 대안이자 리스크 대비 우수한 대안"으로 함께 실을 수 있을 것 같다. BTC 단일자산 트랙은 아직 양(+)의 수익을 내지 못했고, 라우터 경계를 재보정한 v2.2의 결과를 기다리고 있는 상태다.

앞으로 남은 후보로는, 44종목 트랙에서 v4의 top-K를 더 줄이거나(예를 들어 5로) κ 히스테리시스를 이식해 v1의 총수익에 더 근접시키는 실험, BTC 트랙에서 v2.2 결과를 확인한 뒤 여전히 라우터가 나쁘면 학습 시 비용률(δ)을 부풀려 재학습하는 방향을 검토하는 것, 그리고 두 트랙이 정리되는 시점에 맞춰 보고서 섹션 초안을 작성하는 것이 있다.
